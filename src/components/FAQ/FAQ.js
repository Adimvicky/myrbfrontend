import React from 'react';
import { container } from './FAQ.module.css';

const FAQ = () => {
  return (
    <section className={`${container} px-20 py-16`}>
      <h2 className="h2">Frequently Asked Questions</h2>

    </section>
  )
};

export default FAQ;